export type Account = {
    id: string,
    customerId: string,
    balance: number,
    accountTypeId: number,
    isActive: boolean,
    createdOn: Date,
    createdBy: string,
    updatedOn: Date,
    updatedBy: string,
}